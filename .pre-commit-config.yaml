repos:
  - repo: local
    hooks:
      - id: opencode-lint
        name: opencode-lint
        entry: uv run --project opencode_lint opencode-lint --pre-commit --profile fast
        language: system
        pass_filenames: true
        files: '(^|/)([^/]+\.(py|sh|toml|ya?ml)|Dockerfile|Makefile)$'
        stages: [pre-commit]
        require_serial: true

      - id: ruff-format
        name: ruff-format
        entry: uvx ruff format --check
        language: system
        types: [python]
        stages: [pre-commit]

      - id: ruff-check
        name: ruff-check
        entry: uvx ruff check --select E,W,F,I,S,B
        language: system
        types: [python]
        stages: [pre-commit]
