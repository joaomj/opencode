---
description: Verify branch state and create a pull request
---

Use the `create-pull-request` skill to create a pull request for `$ARGUMENTS`.

Do not skip branch comparison, conflict detection, authentication checks, or
the user confirmation required by that skill. Do not rebase or merge
automatically. Use `gh` for GitHub operations.
